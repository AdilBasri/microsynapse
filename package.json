{
  "name": "microsynapse",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "npm --prefix subtracker-frontend start",
    "backend": "npm --prefix subtracker-backend run dev",
    "ai": "cd subtracker-AI && python3 -m uvicorn main:app --reload --port 8000"
  }
}
